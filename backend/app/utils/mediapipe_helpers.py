from __future__ import annotations

from typing import Any


def normalize_landmarks(landmarks: Any) -> Any:
    """Placeholder helper for future MediaPipe-specific cleanup."""
    return landmarks


def landmarks_to_dict(landmarks: Any) -> dict:
    """Convert MediaPipe landmarks to a plain dictionary when needed."""
    return {"landmarks": landmarks}
